#include <paging.h>

/* This is defined somewhere else, but the general one used by the non-arch
 * specific kernel needs to be called this and needs to be in memory.h. */
#define ARCH_FRAME_SIZE FRAME_LEN
